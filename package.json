{
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "typegen": "wrangler types --env-interface CloudflareBindings src/worker.d.ts",
    "fix": "biome check --fix"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "dedent": "^1.6.0",
    "hono": "^4.7.10",
    "uqr": "^0.1.2",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "wrangler": "^4.4.0"
  }
}
